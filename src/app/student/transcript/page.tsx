import TranscriptContent from "@/components/portal/TranscriptContent";

export const metadata = {
  title: "Apply for transcript — Student Portal",
};

export default function TranscriptPage() {
  return <TranscriptContent />;
}
