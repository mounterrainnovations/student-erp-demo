import NoticesContent from "@/components/portal/NoticesContent";

export const metadata = {
  title: "Notices & circulars — Student Portal",
};

export default function NoticesPage() {
  return <NoticesContent />;
}
