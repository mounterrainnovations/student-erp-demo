import StudentProfileContent from "@/components/portal/StudentProfileContent";

export const metadata = {
  title: "Student Profile — Student Portal",
};

export default function ProfilePage() {
  return <StudentProfileContent />;
}
