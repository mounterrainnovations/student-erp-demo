import HelpDeskContent from "@/components/portal/HelpDeskContent";

export const metadata = {
  title: "Help Desk / Grievance System — Student Portal",
};

export default function HelpDeskPage() {
  return <HelpDeskContent />;
}
