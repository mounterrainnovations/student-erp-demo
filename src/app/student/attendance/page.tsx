import AttendanceContent from "@/components/portal/AttendanceContent";

export const metadata = {
  title: "View Attendance — Student Portal",
};

export default function AttendancePage() {
  return <AttendanceContent />;
}
